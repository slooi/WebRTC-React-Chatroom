{
  "name": "reactchat4",
  "version": "1.0.0",
  "description": "Basic webrtc react chatroom app",
  "main": "index.js",
  "scripts": {
    "start": "npm start --prefix server",
    "buildStart": "concurrently \"npm run build\" \"npm run start\"",
    "build": "npm run build --prefix client",
    "dev": "npm run dev --prefix client"
  },
  "author": "samue looi",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
